type POC {
	_id: ID!
	name: String!
	email: [EmailAddress]
	phone: [PhoneNumber]
	createdAt: DateTime!
	updatedAt: DateTime!
	ver: UnsignedInt
}

type POCs {
	items: [POC!]!
	total: UnsignedInt!
}

input POCInput {
	name: String!
	email: [EmailAddress]
	phone: [PhoneNumber]
}
